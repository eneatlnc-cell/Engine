# Engine 开发路线图

> 需求清单持久化 (2026-08-19 与产品对齐)。环境重置后以本文件为准。

## 已完成

- [x] v3.7 年轻化改版 (电流紫 + M3 Expressive)
- [x] v3.8 主题统一时钟 (色彩重影/菜单影子) + 导航变大/变小
- [x] v3.9 白天模式掉帧修复 (状态栏事务量化 + glow 自适应)
- [x] v3.10 FAB 白天重影修复 (无影 FAB + LowBouncy)
- [x] v3.11 布局重构: 顶栏 (变色球 + 🔥) / Spark 三卡片 / X 式设置抽屉 / FAB 双选项 / 顶栏搜索态
- [x] v3.14 群组协议层: GROUP_MSG/GROUP_CTRL 消息类型 + 邀请码目录 (ROOM_*) + 群密钥分发/轮换 + 花名册 + 在场心跳 + 主权顺移
- [x] v3.15 Spark 账本协议 (签名/API 模型, 客户端侧)
- [x] v3.17 60KB 媒体消息链路 + IPC Extra 化 + 防重放内存修复 (详见 README 变更记录)
- [x] v3.17.1 消息预算定稿: 文本 40KB / 媒体 48KB 双轨 + 群组 200 人上限
- [x] v3.18.0 群扇出架构 (P0): GROUP_SUBSCRIBE 订阅 + 群消息单帧上行 + 中继订阅集扇出 (双层令牌桶) + PRESENCE 心跳扇出帧 + 重连重订阅 —— >21 人群发断连与心跳打死问题根治
- [x] v3.18.1 定向增量审计修复: 订阅幂等 (R1) / 控制帧分账限流 128/s 杜绝多群误杀 (R2) / 空群回收桶与订阅集 (R3) / 断连除名索引化 (R4) / 每端独立投递协程 (R5) / GROUP_RATE_LIMITED 关联消息标失败 (R6)
- [x] v3.19.0 群聊 UI 完善 (P1) + 门禁审批 (P2): 群详情页 (成员列表/在场圆点/角色徽章) + 门禁切换 (AUTO/APPROVAL) + 待审队列逐条审批 + 邀请码刷新 + 入群拒绝全局反馈 (GROUP_FULL/JOIN_REJECTED) —— 审批流基础设施一体交付, 详见下方实施记录
- [x] v3.20.0 Spark 表情包系统: 引用式贴纸 (17 字节线格式 vs 59KB 传输, 协议零改动) + 表情面板 (5 列网格/静态缩略图/长按动态预览) + Telegram 风格无气泡 140dp 大表情 (API 28+ 动态播放) + 会话列表预览 —— 详见需求 #4 实施记录
- [x] 表情包资产: Spark 拟人化火花贴纸 60 张 (动态 WebP ≤44KB, 512px 透明底) 已交付

## 下一步开发项

### 1. 群组系统 v3.18 (需求 #2, 当前主战场)

- 用户**无需添加好友**即可发起群组
- 群组生成后可设置:
  - **门禁** (加入条件)
  - **邀请码**: 大写字母 + 数字组成的短号
- **单群成员上限 = 200** (v3.17.1 已定案, 100Mbps 中继带宽推导, 见 `GroupLimits`)
- 现状: 协议层 v3.14 已建成 (密钥分发/轮换/邀请码/在场/主权顺移/200 人闸门), 群聊 UI 骨架已有; **缺中继侧扇出与群级限流** (下述 P0) 与门禁

**P0 — 中继会话级扇出 (定案 A, 2026-08-20; v3.18.0 已实施)**

> 决策记录: 方案 B (TG 式写扩散) 已由产品方排除 —— 写扩散要求中继持久化
> 收件箱队列, 破 "零状态/密文不落盘" 红线。排除后设计空间收敛: 本工程
> 无 P2P 通道 (一切流量经 wss 中继)、客户端零持久化 (离线补投无产品语义),
> 故**唯一可行架构 = 中继侧会话级扇出**。
>
> 实施落地 (v3.18.0): 协议新增 GROUP_SUBSCRIBE / GROUP_FANOUT 帧类型;
> 中继新增 GroupFanoutService (订阅表 + 群级/全局双层令牌桶);
> 客户端群消息单帧上行、心跳单帧扇出、KEY 落地/建群/重连三处订阅。
> 部署注意: **先升中继再发客户端** (旧中继会静默丢弃无 target 的
> GROUP_MSG 扇出帧)。
>
> **定向增量审计 (2026-08-20, v3.18.1 修复):** 范围 = v3.17~v3.18 新增
> 代码 (扇出服务/客户端改造), v3.14.2 已审基线不再全量重审。6 项发现:
> R1 订阅计数器重复订阅虚增 (KEY 轮换即触发, 64 群上限提前触顶) →
> 会话级 gid 集合幂等; R2 **多群客户端被 20 msg/s 业务限流误杀** (21+ 群
> 重连重订阅/心跳扇出即断连, v3.18 所治之病在多群维度回归) → 控制帧
> (GROUP_SUBSCRIBE/GROUP_FANOUT) 分账预算 128/s, 业务预算 20/s 不变;
> R3 空群令牌桶永不回收 (缓慢泄漏) → 末位退订即回收; R4 断连除名全表
> 扫描 O(所有群) → 会话索引 O(本会话群数); R5 单协程顺序投递被慢消费端
> 拖死同群其余端 → 每端独立协程; R6 客户端忽略 GROUP_RATE_LIMITED
> (被拒消息永显已发送) → LIFO 关联最新待决消息标失败。
> 确认无问题 3 项: 防重放三计数空间隔离正确; 单帧化不改计费口径
> (发送方 1 Spark/条与扇出解耦); pendingFanout 生命周期有界。
> 回归: 冒烟 8/8 + 审计回归 7/7 (多群突发存活/幂等/64 上限/业务限流不回归)。

**现状硬伤 (两处, 均为客户端逐成员扇出):**

1. **群消息**: 发送方一口气发 N-1 帧 GROUP_MSG (同一份群密文),
   撞中继单连接 20 msg/s 限流 → **>21 人的群发一条消息即被断连**;
   且 40KB 满额文本 × 199 ≈ 10.6MB 上行, 手机上行带宽扛不住。
2. **在场心跳** (v3.14.1, `groupPresenceTick`): 每 30s 每成员向其他
   全员逐个发 PRESENCE → 每连接单 tick 突发 199 帧 (同样撞 20 msg/s);
   200 人全在线时中继聚合 ≈ 39,800 帧/30s ≈ 1,327 帧/s ——
   **大群即使无人说话也会被心跳流量打死**。

**定案 A 实现规格 (中继仍零落盘, 仅增会话态内存表):**

协议层 (core-protocol):
- 新增帧类型 `GROUP_SUBSCRIBE(groupId)`: 认证后 / 入群 KEY 落地后 /
  WebSocket 重连后发送。groupId 为不可猜测 UUID, 仅经 E2E 密钥分发
  通道扩散 → **能订阅即持有群秘密, 订阅本身即鉴权** (中继无需验证
  成员身份, 只路由密文; 非成员订阅者拿到的只是无法解密的密文)
- `GROUP_MSG` 改单帧上行: 带 groupId、不带 target, 中继向订阅集扇出
- **PRESENCE 从 GROUP_CTRL (1:1 会话密钥) 迁移为群密钥扇出帧**:
  每成员每 tick 仅 1 帧上行; JOIN/KEY/ROSTER 等定向信令维持 1:1 不变
- AAD / SeqGuard 无需变更 (群消息本就群密钥加密、按 gid:source 记数)

中继层 (relay-server):
- `groupId → MutableSet<Session>` 内存订阅表, 与现有 ConnectionRegistry
  (指纹→会话) 同性质: 会话态、断开即除名 (复用 `unregisterIfOwner` 钩子)、
  进程重启即清零 → **零落盘红线不破**
- 护栏: 每连接订阅数上限 (64 群); 群级令牌桶 10 msg/s + 2MB/s egress
  (吸收 200 人贴纸突发 ≈15.6MB ≈1.25s 满管); 全局 egress 上限防止
  大群饿死 1:1 流量
- 兼容回退: 订阅集为空的 GROUP_MSG 退化为逐成员点对点 (旧客户端共存期)

客户端层 (app-a):
- `sendGroupMessage`: N-1 帧循环 → 1 帧 (targets 计算删除)
- `groupPresenceTick`: 199 帧突发 → 1 帧; 大群 (≥100 人) 心跳间隔
  30s → 60s 自适应 (200 人全在线时 presence 下行 ≈2MB/s, 占管 16%,
  加倍间隔减半)
- 重连后全群重订阅 (现有 HELLO 重认证流程已具备挂载点)
- 计费不变: 发送方 1 Spark/条 —— 扇出是传输行为, 不是计费事件

**已评估并排除的变体 (留档):**

| 变体 | 排除原因 |
|------|---------|
| B 写扩散 (TG 式收件箱) | 中继须持久化收件箱 → 破零状态红线 (产品拍板排除) |
| A2 客户端树状接力扇出 | 中继总带宽不变 (N-1 帧仍过中继), 白加两级时延; 接力者在线性不确定; 转发计费归属混乱 |
| A' 目标列表帧 (每条 GROUP_MSG 附 199 个指纹 ≈+13KB) | 零新状态但每条消息冗余上传目标表; 聊天群 10 msg/s 时 +130KB/s/发送者; 仅作极端保守回退保留 |

**带宽核算 (100Mbps ≈ 12.5MB/s, 200 人群, 定案 A 生效后):**
| 流量 | 扇出 egress | 占满管时间/占比 |
|------|-------------|----------------|
| 典型文本 (~1.5KB 帧) | ~300KB | 2.4% |
| 40KB 满额文本 (~54KB 帧) | ~10.6MB | 0.85s 满管 |
| 44KB 贴纸 (~79KB 帧) | ~15.6MB | 1.25s 满管 |
| 在场心跳 (60s 间隔) | ~60MB/60s ≈ 1MB/s | 8% 持续 |

结论: 200 上限成立 (人均 ~62KB/s 扇出预算, 最坏单条 1.25s 满管由群级
令牌桶吸收); 500 人单条贴纸 ≈3.4s 满管 + 心跳翻倍, 两个并发大群互相
饿死, 不可行。上限维持 200 不变。

**P1 — 群聊 UI 完善:** ~~群成员列表页 / 群设置 (门禁/邀请码展示与刷新) / 入群审批流 UI / 满员与拒绝提示 (GROUP_FULL)~~ ✅ 已完成 (v3.19.0)
**P2 — 门禁:** ~~邀请码模式之上加 "群主审批" 开关 (JOIN_REQ → 群主确认 → KEY, 协议已预留 JOIN_RESP)~~ ✅ 已完成 (v3.19.0, 与 P1 共享审批流基础设施一并交付)

> **v3.19.0 实施落地 (P1 + P2 一体交付, 审批流基础设施共享):**
> · **群详情页** (新路由 `groupDetail/{groupId}`): 入口 = 群聊页顶栏标题点击 +
>   群组管理页卡片设置键; 群名片 (N/200 成员 + 密钥版本) + 成员列表
>   (在场圆点: 页面级 15s 刷新拍单定时器驱动, 200 人群不成百协程;
>   角色徽章 群主/管理)。
> · **门禁 (P2)**: `EngineGroup.gateMode` (AUTO=在线即批, v3.14 默认行为 /
>   APPROVAL=人工审批); 仅群主本地语义, 不入协议帧 —— 门禁在群主侧执行,
>   群纯内存态模式随群主存续。JOIN_REQ 按 gateMode 分流: 满员先拒
>   (GROUP_FULL) → APPROVAL 入待审队列 → AUTO 直接 admitJoin。
> · **审批流**: 群主侧 `pendingJoinRequests` StateFlow ((groupId,fp) 去重,
>   TTL 10min, 上限 100 条, 群解散/退群/被移除时清空); 群详情页待审卡
>   (同意 → admitJoin 复用准入逻辑; 拒绝 → JOIN_RESP approved=false
>   reason=JOIN_REJECTED); 同意时满员竞态防护 (排队期间已满转 GROUP_FULL)。
>   群组管理卡片显示 "N 待审" 角标。
> · **拒绝反馈 (P1-d)**: 申请者侧 JOIN_RESP approved=false → 全局对话框
>   (MainActivity 挂载, 拒绝异步到达时入群对话框早已关闭): GROUP_FULL →
>   "群已满员 (200 人上限)"; JOIN_REJECTED → "入群申请被群主拒绝"。
> · **邀请码刷新**: 群详情页邀请码卡 (大号码 + 复制 + 刷新 + 中继登记状态);
>   刷新生成新码并重新登记, 旧码 24h 自然过期。
> · 协议: `GroupErrorCodes.JOIN_REJECTED` (审批拒绝语义码); 其余帧型不变,
>   JOIN_RESP/批准路径复用 v3.14 已预留字段。
> 验证: app-a/core-protocol/relay-server 编译通过; 协议单测通过;
> 中继冒烟 8/8 + 审计回归 7/7 (无回归)。

### 2. Spark 加密货币 (需求 #3)

- $SPARK 为产品方发行的加密货币
- 计费规则: **发一条消息 = 1 $SPARK** (文字/图片/表情同价), 文本 ≤ 40KB / 媒体 ≤ 48KB
- 获取渠道 (仅两种):
  1. **每天凌晨登录自动领取 1000 $SPARK**
  2. **从钱包充值**
- 服务端部署在 **香港 VPS** (与中继同机或邻机): spark-ledger 独立有状态服务 (协议 v3.15 已定: 签名/防重放/批量结算)
- 现状: 协议模型 + 客户端预扣已就位, 服务端实现/部署未开始
- 待产品方提供: 官网 URL

### 3. 表情包系统 (需求 #4)

- 参考 Telegram **超大号动态表情**
- ~~资产已交付: Spark 60 张 (动态 WebP ≤44KB + 静态 PNG + manifest, `spark-stickers/`)~~
- ~~剩余: **表情面板 UI** (替换键盘) + 大表情消息渲染 (超大气泡) + 发送链路~~ ✅ 已完成 (v3.20.0)
- ~~可选: 矢量 .tgs 重制 (Lottie rigging, 纯设计工作量)~~ (WebP 方案已定案, 不再重制)

> **v3.20.0 实施落地 —— 引用式贴纸 (架构决策):**
> 原计划的 "贴纸按 48KB 媒体轨传输" 方案**弃用**。表情包随 APK 打包
> (60 张动态 WebP 2.47MB + stickers.json 索引), 收发双方目录必然一致,
> 消息线上只传 `[spark:st001_hello]` 引用 (17 字节 vs 59KB Base64 密文):
> · 单条贴纸与普通文本同价 —— 群扇出 egress (帧字节 × 接收端数) 从
>   最坏 15.6MB 降至百字节级, 令牌桶压力归零;
> · 复用 1:1 / 群消息 / 扇出三条既有加密链路, **协议零改动**;
> · 引用仍经 E2E 加密, 中继只见密文;
> · 兼容降级: 接收侧校验 ID 在本机目录, 未收录 (版本错配) → 按普通
>   文本渲染, 无静默丢消息。
>
> 组件: `StickerCatalog` (目录/线格式/缩略图 LRU 48 张) +
> `StickerImage` (API 28+ AnimatedImageDrawable 动态播放 / 26-27
> BitmapFactory 静态首帧, 零图片库依赖; 离屏自动 stop) +
> `StickerPickerPanel` (5 列网格, 面板静态缩略图防 60MB 解码洪水,
> 点击即发, 长按全屏动态预览) + `StickerBubble` (Telegram 风格无气泡
> 140dp 大表情, 群名/时间行/状态图标保留, 长按仍可标记)。
> 会话列表预览: "👋 Spark 表情" (1:1 与群一致)。
> 验证: assembleDebug 通过, APK 含 60 webp + 索引 (20.8MB)。

### 4. 标记物 (需求 #5 / 拍板 #3)

- 定位: 用户觉得需要**持久保存**的文字/图片/其他
- 架构约束: 应用端对端**无数据库**, 数据全部在内存 (arm),
  杀死进程即清零
- 实现: 标记 = 从 arm **转存到手机本地存储** (用户可清理)
- 涉及: 消息长按菜单"标记" / 本地标记仓 (文件式存储) / 设置页标记物列表
- 现状: 设置抽屉入口已就位, 存储层未动

### 5. 指纹短码添加联系人 (拍板 #5)

- 应用内添加联系人需 **@对方指纹** 才能搜索添加
- 现状: 搜索框已支持 @ 前缀过滤 (ChatListScreen 400 行附近已有
  `searchQuery.startsWith("@")` 逻辑)

## 技术备忘

- FLAG_SECURE 全局开启 → 窗口仅 GPU 合成, 一切透明度渐隐动画
  慎配 elevation 投影 (v3.10 教训)
- 主题唯一时钟在 `ThemeSync.kt`, 新增颜色角色必须进 `lerpColorScheme`
- 所有悬浮层 (菜单/FAB/弹窗) 统一**无影 + 1dp 发丝描边**语言
- 数据层无数据库: 联系人/会话在 `InMemoryContactStore` (内存),
  绑定身份在 `BoundIdentityStore` (SharedPreferences)

## 密钥生命周期策略 (v3.13 定案)

| 事件 | Engine 侧 | Vault 侧 |
|------|----------|---------|
| 正常绑定 | 生成密钥对 → QR/Extra 移交私钥 → 存公钥+指纹 | Keystore 加密落盘 (按包名路由), 导入即替换旧绑定(带警告) |
| **Engine 清除数据** | 本地身份全丢, 进入 Idle → **首选"从 Vault 恢复身份"** | 绑定仍在 → restore 回送公钥 → **同一 DID 回归** |
| Vault 无绑定 (从未绑过/也清了) | 回 NO_BINDING → 引导生成新密钥对 | — |
| 换机 (Vault 也新装) | 无处恢复 → 新身份 (旧 DID 需重新互加) | — |
| 重新绑定 (危险操作) | 二次确认 → 新密钥对 | 旧绑定被替换 (红色警告) |

原则: **私钥只进不出 Vault**; 恢复流程只回送公钥 (非秘密材料),
回调签名即私钥持有证明。
